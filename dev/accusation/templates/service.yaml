apiVersion: v1
kind: Service
metadata:
  name: {{ include "accusation.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "accusation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "accusation.selectorLabels" . | nindent 4 }}
