apiVersion: v1
kind: Service
metadata:
  name: {{ include "notice.fullname" . }}
  labels:
    {{- include "notice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "notice.selectorLabels" . | nindent 4 }}
