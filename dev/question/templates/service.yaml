apiVersion: v1
kind: Service
metadata:
  name: {{ include "question.fullname" . }}
  labels:
    {{- include "question.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "question.selectorLabels" . | nindent 4 }}
